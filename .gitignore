# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.eggs/
MANIFEST

# Virtual environments
.venv/
venv/
env/
ENV/

# Test artifacts
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
coverage.xml
*.cover

# Type checkers
.mypy_cache/
.pyre/
.pytype/
.ruff_cache/

# Env files (NEVER commit secrets)
.env
.env.local
.env.*.local
!.env.example

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Local user profiles & overrides
.polysearch/
config/local-*.yaml
config/*.local.yaml

# Local research output (users may regenerate; don't track)
# Anchored to repo root so we don't match src/polysearch/output/ (the package).
/reports/
/output/

# Lockfiles from alternate package managers (we use pip+pyproject.toml)
uv.lock
poetry.lock
Pipfile.lock

# Logs
*.log
unknown_domains.log
